version: '3.3'
services:
  mongo:
    image: mongo
    ports:
    - "27017:27017"
  web:
    build: .
    command: npm start
    environment:
      - CONFIG=dev
    volumes:
      - .:/usr/src/app
    ports:
      - "3000:3000"
    depends_on:
      - mongo